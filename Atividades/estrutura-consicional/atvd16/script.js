// Peça a base e a altura, calcule e mostre a área.

let a = Number(window.prompt("Calcular a área do retângulo.\n==========================\nDigite a base: "))
let b = Number(window.prompt("Digite a Altura: "))

alert(`A área do retângulo é: ${a*b}`)